# tests/ — Test suite

TDD tests live here from **Stage 3** onward, mirroring `src/` structure, covering happy and
error paths, with shared fixtures in `conftest.py`. Coverage target ≥85% (`docs/QUALITY.md`).
Empty in Stage 0 because there is no code to test yet.
